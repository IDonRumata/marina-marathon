# 🚀 GIT КОМАНДЫ ДЛЯ ДЕПЛОЯ

Скопируйте и выполните эти команды в терминале для быстрого деплоя.

---

## ✅ ПЕРЕД НАЧАЛОМ

Убедитесь что:
- [ ] Git установлен (`git --version`)
- [ ] У вас есть GitHub аккаунт
- [ ] Вы находитесь в папке проекта `marina-marathon`

---

## 📋 КОМАНДЫ ДЛЯ КОПИРОВАНИЯ

### ШАГ 1: Инициализация Git репозитория

```bash
# Переходим в папку проекта (если не в ней)
cd marina-marathon

# Инициализируем Git
git init

# Добавляем все файлы
git add .

# Делаем первый коммит
git commit -m "Initial commit: Marina Marathon Landing Page with secure Telegram integration"

# Устанавливаем основную ветку main
git branch -M main
```

---

### ШАГ 2: Подключение к GitHub

**2.1. Создайте репозиторий на GitHub:**
1. Откройте https://github.com/new
2. Название: `marina-marathon`
3. Выберите: **Private** ✅ (для безопасности)
4. НЕ добавляйте README, .gitignore (уже есть)
5. Нажмите "Create repository"

**2.2. Подключите репозиторий:**

```bash
# ЗАМЕНИТЕ YOUR_USERNAME на ваш GitHub username!
git remote add origin https://github.com/YOUR_USERNAME/marina-marathon.git

# Проверяем что remote добавлен
git remote -v
```

**Должно показать:**
```
origin  https://github.com/YOUR_USERNAME/marina-marathon.git (fetch)
origin  https://github.com/YOUR_USERNAME/marina-marathon.git (push)
```

---

### ШАГ 3: Загрузка кода на GitHub

```bash
# Отправляем код на GitHub
git push -u origin main
```

**При первой отправке Git может попросить:**
- Ввести GitHub логин
- Ввести пароль (или Personal Access Token)

---

## 🔑 ЕСЛИ НУЖЕН PERSONAL ACCESS TOKEN

GitHub больше не принимает пароли для git push. Нужен токен:

1. Откройте: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Classic"
3. Выберите scopes: `repo` (full control)
4. Нажмите "Generate token"
5. **Скопируйте токен** (он покажется только один раз!)
6. Используйте его вместо пароля при `git push`

---

## ✅ ПРОВЕРКА

После `git push` откройте:
```
https://github.com/YOUR_USERNAME/marina-marathon
```

Должны увидеть все файлы проекта! ✨

---

## 🔄 КОМАНДЫ ДЛЯ ОБНОВЛЕНИЯ КОДА

Когда будете вносить изменения:

```bash
# 1. Проверяем статус
git status

# 2. Добавляем измененные файлы
git add .

# 3. Коммитим с описанием
git commit -m "Описание изменений"

# 4. Отправляем на GitHub
git push
```

Vercel автоматически задеплоит изменения!

---

## 🛠️ ПОЛЕЗНЫЕ GIT КОМАНДЫ

```bash
# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения в файлах
git diff

# Отменить изменения в файле (до коммита)
git checkout -- имя_файла

# Посмотреть все ветки
git branch -a

# Создать новую ветку
git checkout -b имя_ветки

# Переключиться на ветку
git checkout имя_ветки
```

---

## ❌ ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Ошибка: "remote origin already exists"

```bash
# Удалите старый remote
git remote remove origin

# Добавьте новый
git remote add origin https://github.com/YOUR_USERNAME/marina-marathon.git
```

---

### Ошибка: "failed to push some refs"

```bash
# Сначала получите изменения с GitHub
git pull origin main --allow-unrelated-histories

# Потом отправьте свои
git push -u origin main
```

---

### Ошибка: "Authentication failed"

**Решение:** Используйте Personal Access Token вместо пароля (см. выше)

---

### Забыли добавить файл в .gitignore

```bash
# Удаляем файл из Git (но не с диска)
git rm --cached имя_файла

# Добавляем в .gitignore
echo "имя_файла" >> .gitignore

# Коммитим
git add .gitignore
git commit -m "Update .gitignore"
git push
```

---

## 🎯 ПОЛНЫЙ ПРОЦЕСС (КРАТКО)

```bash
# 1. Инициализация
cd marina-marathon
git init
git add .
git commit -m "Initial commit"
git branch -M main

# 2. Подключение GitHub (создайте репозиторий сначала!)
git remote add origin https://github.com/YOUR_USERNAME/marina-marathon.git

# 3. Отправка
git push -u origin main

# ✅ ГОТОВО! Переходите к деплою на Vercel
```

---

## 📝 РЕКОМЕНДАЦИИ

### ✅ ХОРОШИЕ ПРАКТИКИ:

- Делайте коммиты часто (после каждой логической единицы работы)
- Пишите понятные сообщения коммитов
- Используйте `.gitignore` для секретных файлов
- Делайте приватные репозитории для проектов с токенами

### ❌ ПЛОХИЕ ПРАКТИКИ:

- Не коммитьте `.env` файлы с реальными токенами
- Не делайте коммиты с сообщениями типа "fix", "test", "aaa"
- Не добавляйте в Git огромные файлы (>100MB)
- Не делайте публичные репозитории с приватными данными

---

## 🔐 БЕЗОПАСНОСТЬ

### Что ДОЛЖНО быть в Git:
✅ Исходный код (HTML, CSS, JS)
✅ Конфигурационные файлы (vercel.json)
✅ Документация (README.md и т.д.)
✅ .gitignore
✅ .env.example (БЕЗ реальных токенов!)

### Что НЕ ДОЛЖНО быть в Git:
❌ .env (с реальными токенами)
❌ node_modules
❌ Системные файлы (.DS_Store и т.д.)
❌ Логи и временные файлы

---

## 📚 ДОПОЛНИТЕЛЬНО

### Хотите узнать больше о Git?

- **Git официальная документация:** https://git-scm.com/doc
- **GitHub Guides:** https://guides.github.com
- **Интерактивный тренажер:** https://learngitbranching.js.org

### Визуальные клиенты Git (если не любите терминал):

- **GitHub Desktop:** https://desktop.github.com
- **GitKraken:** https://www.gitkraken.com
- **SourceTree:** https://www.sourcetreeapp.com

---

## ✨ ГОТОВО!

После выполнения всех команд:
1. ✅ Код на GitHub
2. ✅ Репозиторий приватный
3. ✅ .env файлы защищены .gitignore
4. ✅ Можно деплоить на Vercel!

**Следующий шаг:** Откройте **README.md** раздел "ШАГ 3: Деплой на Vercel"

---

**Удачи! 🚀**
